import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { DefaultAvatarIcon } from '@/components/icons/DefaultAvatarIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/default-avatar" />

<Story title={'default-avatar'}>
    <IconViewer>
        <DefaultAvatarIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'default-avatar'}
        componentCode={renderToString(<DefaultAvatarIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <DefaultAvatarIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
