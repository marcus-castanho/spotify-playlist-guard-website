import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { MoonIcon } from '@/components/icons/MoonIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/moon" />

<Story title={'moon'}>
    <IconViewer>
        <MoonIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'moon'}
        componentCode={renderToString(<MoonIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <MoonIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
