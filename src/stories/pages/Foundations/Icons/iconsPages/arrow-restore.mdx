import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { ArrowRestoreIcon } from '@/components/icons/ArrowRestoreIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/arrow-restore" />

<Story title={'arrow-restore'}>
    <IconViewer>
        <ArrowRestoreIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'arrow-restore'}
        componentCode={renderToString(<ArrowRestoreIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <ArrowRestoreIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
