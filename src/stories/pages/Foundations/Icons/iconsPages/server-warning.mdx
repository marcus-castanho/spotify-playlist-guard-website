import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { ServerWarningIcon } from '@/components/icons/ServerWarningIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/server-warning" />

<Story title={'server-warning'}>
    <IconViewer>
        <ServerWarningIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'server-warning'}
        componentCode={renderToString(<ServerWarningIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <ServerWarningIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
