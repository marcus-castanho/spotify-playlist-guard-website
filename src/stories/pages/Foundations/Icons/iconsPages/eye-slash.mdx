import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { EyeSlashIcon } from '@/components/icons/EyeSlashIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/eye-slash" />

<Story title={'eye-slash'}>
    <IconViewer>
        <EyeSlashIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'eye-slash'}
        componentCode={renderToString(<EyeSlashIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <EyeSlashIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
