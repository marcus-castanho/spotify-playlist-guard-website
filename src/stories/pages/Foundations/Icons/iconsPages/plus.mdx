import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { PlusIcon } from '@/components/icons/PlusIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/plus" />

<Story title={'plus'}>
    <IconViewer>
        <PlusIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'plus'}
        componentCode={renderToString(<PlusIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <PlusIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
