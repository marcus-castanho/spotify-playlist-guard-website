import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { MusicalNoteIcon } from '@/components/icons/MusicalNoteIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/musical-note" />

<Story title={'musical-note'}>
    <IconViewer>
        <MusicalNoteIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'musical-note'}
        componentCode={renderToString(<MusicalNoteIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <MusicalNoteIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
