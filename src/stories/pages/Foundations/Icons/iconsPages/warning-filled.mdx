import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { WarningFilledIcon } from '@/components/icons/WarningFilledIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/warning-filled" />

<Story title={'warning-filled'}>
    <IconViewer>
        <WarningFilledIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'warning-filled'}
        componentCode={renderToString(<WarningFilledIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <WarningFilledIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
