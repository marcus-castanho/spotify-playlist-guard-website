import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { FloppyDiskIcon } from '@/components/icons/FloppyDiskIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/floppy-disk" />

<Story title={'floppy-disk'}>
    <IconViewer>
        <FloppyDiskIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'floppy-disk'}
        componentCode={renderToString(<FloppyDiskIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <FloppyDiskIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
