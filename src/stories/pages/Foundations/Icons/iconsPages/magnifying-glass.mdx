import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { MagnifyingGlassIcon } from '@/components/icons/MagnifyingGlassIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/magnifying-glass" />

<Story title={'magnifying-glass'}>
    <IconViewer>
        <MagnifyingGlassIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'magnifying-glass'}
        componentCode={renderToString(<MagnifyingGlassIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <MagnifyingGlassIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
