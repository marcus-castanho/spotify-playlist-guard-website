import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { SunIcon } from '@/components/icons/SunIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/sun" />

<Story title={'sun'}>
    <IconViewer>
        <SunIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'sun'}
        componentCode={renderToString(<SunIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <SunIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
