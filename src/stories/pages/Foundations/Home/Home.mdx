import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { FoundationsIndex } from './components/FoundationsIndex.tsx';

<Meta title="Home" />

<Story title={'Club - A system design for Spotify Playlist Guard UI'}>
    <p>{`Club is Spotify Playlist Guard’s design system. The system includes foudations for building the UI of applications such as color schemes, spacing, typography, icons, box shadows and breakpoints for screens in web applications. This storybook also presents the documentation for the React components used across the app.`}</p>
    <FoundationsIndex />
</Story>
